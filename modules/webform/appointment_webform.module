<?php
/**
 * @file
 * Appointment Webform module.
 */

/**
 * Implements hook_appointment_booking_type_info().
 */
function appointment_webform_appointment_booking_type_info() {
  $types = array();
  $types['webform'] = array(
    'label' => t('Webform booking'),
    'description' => t('Link appointment bookings to webforms'),
    'module' => 'appointment_webform',
  );
  return $types;
}
